# College grizzly manager backend

## To install

* assign server.py as FLASK_APP
* run `flask db init` to initialize database and migrations
* run `flask db upgrade` to create database
* run `flask geocodes save_in_database` to add states, counties, places 
  and consolidated cities to the database

## To start

Run `flask run`
